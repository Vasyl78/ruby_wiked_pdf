#!/usr/bin/env ruby
# frozen_string_literal: true

require "./config/cli_config"
require "benchmark"
require "logger"

cli = CLI.new
result = 0

time = Benchmark.realtime { result = cli.run }
logger = Logger.new($stdout)

logger.debug("Finished in #{time} seconds") if cli.options[:debug]
exit result
